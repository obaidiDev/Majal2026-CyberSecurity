# The root .gitignore excludes *.log — but these ARE the lab evidence.
!access.log
!auth.log
!source/majal-day4-raw.log

# Instructor-only. THIS REPOSITORY IS PUBLIC — do not commit any of these.
#   ANSWER_KEY.md    every answer in plain text
#   SOLUTIONS.md     the worked walkthrough — answers plus the derivations,
#                    including which fields are 'cannot be determined'
#   build/           make_fields.py holds the plaintext answers the console
#                    hashes to hide; verify.py names every attacker address
ANSWER_KEY.md
SOLUTIONS.md
build/
